{
  "name": "reversed-text-normalizer",
  "version": "1.0.0",
  "description": "Chrome MV3 extension that detects and normalizes reversed text on webpages.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:e2e": "node --test test/e2e/*.e2e.mjs",
    "test:all": "npm test && npm run test:e2e",
    "build:icons": "node tools/gen-icons.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^29.1.1",
    "puppeteer-core": "^25.1.0"
  }
}
