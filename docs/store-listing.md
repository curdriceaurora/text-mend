# Store Listing — Text Mend

**Product name:** Text Mend

A Chrome MV3 extension that repairs reversed, mojibake, and garbled article text locally and
presents a clean reader view. Local-only, no accounts, no tracking.

## Per-platform listing copy

| Platform | Listing name | Subtitle / tagline |
|---|---|---|
| **Chrome Web Store** | Text Mend | Text Mend: Repair Reversed and Garbled Articles |
| **Firefox Add-ons (AMO)** | Text Mend | Text Mend: Clean Reader for Broken Article Text |
| **Safari Extensions** | Text Mend | Text Mend Reader |

> Manifest `name` is **"Text Mend"** (the brand). The platform subtitles above are marketing
> metadata entered in each store's dashboard (Chrome "summary", AMO "summary", App Store
> "subtitle"), not fields in `manifest.json`.

**Short description (manifest, ≤132 chars):**
"Repair reversed, mojibake, and garbled article text — locally, in a clean reader view."

## Availability check (2026-06-09)

Performed before finalizing the name. **Not legal advice** — a formal trademark clearance
(USPTO TESS + counsel) is recommended before any TM filing or major brand spend.

| Channel | Result |
|---|---|
| Chrome Web Store name | Clear — no "Text Mend" extension (nearby: *Mend Advise* by Mend.io, *Text Mode*, *TextClean*). |
| Firefox AMO name | Clear — no "Text Mend"/"TextMend". |
| Safari / app stores | No "Text Mend" found. |
| Trademark "Text Mend" | No identical registered/common-law mark found. |
| Trademark "Mend" | ⚠️ Mend.io (ex-WhiteSource) is a registered brand in application-security software and ships a browser extension ("Mend Advise"). Different category (enterprise AppSec vs. consumer reading utility), but adjacent software/extension space — clear with counsel before filing. |
| Domain `textmend.com` | Taken (registered 2017, PublicDomainRegistry; appears parked, no live product). |
| Domains `.app` / `.io` | Not verified programmatically; no product exists, likely available — confirm at a registrar. Candidates: `textmend.app`, `textmend.io`, `usetextmend.com`, `gettextmend.com`. |

**Conclusion:** Store names are clear on all three platforms; "Text Mend" is safe to use as the
extension name. Open follow-ups (owner's call): pick an available domain, and run a formal
trademark clearance given the Mend.io adjacency before any filing.
